#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int n,temp;
    vector<int> ans;
    cin>>n;
    for (int i=0;i<n;i++)
    {
        scanf("%d ",&temp);
        ans.push_back(temp);
    }
    sort(ans.begin(),ans.end());
    for (int i=0;i<n;i++){
        printf("%d ",ans[i]);
    }
    return 0;
}
